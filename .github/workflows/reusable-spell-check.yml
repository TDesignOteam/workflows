name: spell-check
on:
  workflow_call:
    inputs:
      config:
        required: false
        type: string
        default: ''

jobs:
  check:
    name: Typos
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6.0.2

      - name: Check spelling
        uses: crate-ci/typos@v1.46.1
        with:
          config: ${{ inputs.config }}
